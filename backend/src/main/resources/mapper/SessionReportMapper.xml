<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.speaker.app.mapper.SessionReportMapper">

    <resultMap id="ReportMap" type="com.speaker.app.model.entity.SessionReport">
        <id column="id" property="id"/>
        <result column="session_id" property="sessionId"/>
        <result column="pronunciation_score" property="pronunciationScore"/>
        <result column="grammar_score" property="grammarScore"/>
        <result column="coherence_score" property="coherenceScore"/>
        <result column="fluency_score" property="fluencyScore"/>
        <result column="ideas_score" property="ideasScore"/>
        <result column="overall_band" property="overallBand"/>
        <result column="detailed_feedback" property="detailedFeedback"/>
        <result column="suggestions_json" property="suggestionsJson"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO session_reports (session_id, pronunciation_score, grammar_score, coherence_score,
            fluency_score, ideas_score, overall_band, detailed_feedback, suggestions_json)
        VALUES (#{sessionId}, #{pronunciationScore}, #{grammarScore}, #{coherenceScore},
            #{fluencyScore}, #{ideasScore}, #{overallBand}, #{detailedFeedback}, #{suggestionsJson})
    </insert>

    <update id="update">
        UPDATE session_reports
        SET pronunciation_score = #{pronunciationScore},
            grammar_score = #{grammarScore},
            coherence_score = #{coherenceScore},
            fluency_score = #{fluencyScore},
            ideas_score = #{ideasScore},
            overall_band = #{overallBand},
            detailed_feedback = #{detailedFeedback},
            suggestions_json = #{suggestionsJson}
        WHERE id = #{id}
    </update>

    <select id="findBySessionId" resultMap="ReportMap">
        SELECT * FROM session_reports WHERE session_id = #{sessionId}
    </select>
</mapper>
